import React from "react";
import ClassroomPage from "../pages/classroomPage"; // ✅ Uses ClassroomPage

const Classroom = () => {
  return <ClassroomPage />; // ✅ Renders the full ClassroomPage with Create Classroom button
};

export default Classroom;
